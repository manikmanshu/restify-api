function initialize(server) {
    // User
    userRoutes(server);
}

function userRoutes(server) {
    // POST     /api/users
    // PUT      /api/users/:id
    // GET      /api/users
    // GET      /api/users/:id
    // DELETE   /api/users/:id
}

module.exports = { initialize }